<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="description" content="A front-end template that helps you build fast, modern mobile web apps.">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0">
    <title><%= CONFIG['name'] %>: Status</title>

    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto:regular,bold,italic,thin,light,bolditalic,black,medium&amp;lang=en">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="https://code.getmdl.io/1.1.3/material.cyan-light_blue.min.css">
    <style>
      .mdl-layout {
      	align-items: center;
        justify-content: center;
      }
      .mdl-layout__content {
      	padding: 24px;
      	flex: none;
      }
      .base-card-wide.mdl-card {
        width: 512px;
      }
      .material-icons.green { color: #7cb342; }
      .material-icons.orange { color: #FB8C00; }
      .material-icons.red { color: #e53935; }
    </style>
  </head>
  <body>
    <div class="mdl-layout mdl-js-layout mdl-color--grey-100">
    	<main class="mdl-layout__content">
        <div class="base-card-wide mdl-card mdl-shadow--6dp">
          <div class="mdl-card__title mdl-color--blue mdl-color-text--white">
            <h2 class="mdl-card__title-text"><%= CONFIG['name'] %>: Status</h2>
          </div>
          <div class="mdl-card__supporting-text">
            <ul class="mdl-list">
              <% @services.each do |service| %>
                <li class="mdl-list__item mdl-list__item--three-line">
                  <span class="mdl-list__item-primary-content">
                    <i class="material-icons mdl-list__item-icon <%= service['color'] %>"><%= service['icon'] %></i>
                    <span><%= service['name'] %></span>
                    <span class="mdl-list__item-text-body">
                      <% if service['status'] == :down %>
                        Service is curently down. We're working on a solution.
                      <% elsif service['status'] == :warning %>
                        Service is currently disrupted. We're working on a solution.
                      <% else %>
                        Service is currently normal.
                      <% end %>
                    </span>
                  </span>
                </li>
              <% end %>
            </ul>
          </div>
        </div>
    	</main>
    </div>
  <script src="https://code.getmdl.io/1.1.3/material.min.js"></script>
 </body>
</html>
